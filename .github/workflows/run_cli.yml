name: Integration test cases
on: push
jobs:
    Build_fails_for_Invalid_command:
#       strategy:
#             matrix:
#                 platform: [ubuntu-latest, ubuntu-18.04]
      runs-on: ubuntu-latest
#       continue-on-error: true
      steps:
#         - name: cli run
#           run: |
#             gh version
#             echo ${{secrets.GH_TOKEN}}
#             export GITHUB_TOKEN=${{secrets.GH_TOKEN}}
        
      #      export GITHUB_WORKSPACE='./test'        
        - name: 'Upload Artifact'
          uses: actions/upload-artifact@v2
          with:
            name: my-artifact
            path: logs.txt
        - name: test logs
          run : |
            export GITHUB_TOKEN=${{secrets.GH_TOKEN}}
            gh repo clone Viranch526/test_workflow
            cd test_workflow
            gh run view --job=2882598774 --log | grep Warning
            

